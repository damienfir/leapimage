leapimage
=========

This proof of concept uses the [Leap Motion](http://www.leapmotion.com) controller to resize and move an image displayed with OpenGL.


Installation
------------

The only requirement is the Leap Motion SDK and the driver for the controller.
